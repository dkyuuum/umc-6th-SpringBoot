package com.example.umc.service.ReviewService;

import com.example.umc.domain.Review;
import org.hibernate.query.Page;

public interface ReviewQueryService {
//     Page<Review> getUserReviewList(int memberId, int page);
}
